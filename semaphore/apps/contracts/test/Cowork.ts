import { ethers, run } from "hardhat"
import { Cowork, GateKeeper, PrVerifier } from "../build/typechain"

describe("Cowork", async () => {
    let coworkContract: Cowork
    let gateKeeperContract: GateKeeper
    let prVerifierContract: PrVerifier // needed for the gatekeeper

    beforeEach(async () => {})
})
